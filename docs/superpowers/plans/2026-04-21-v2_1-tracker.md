# v2.1.0 PR tracker

**Purpose:** cross-session handoff. Plan lives at
`docs/superpowers/plans/2026-04-21-v2_1-implementation.md` with `- [ ]`
checkboxes per TDD step. This file carries **PR-level** state (branch,
HEAD, GitHub PR number, review status) so a fresh session after `/new`
can resume in one hop without re-scanning every checkbox.

**Update rule:** every commit that moves a PR forward updates the
corresponding row here. Every merge clears the `head` column and sets
`status = merged <sha-on-main>`. Tracker lives on `main` so it is
always the current truth regardless of feature-branch activity.

## Status table

| PR | Title | Status | Branch | HEAD | GH PR | Notes |
|----|-------|--------|--------|------|-------|-------|
| A | SymbolKind extension + language dispatcher | **merged 4d72379** | — | — | [#19](https://github.com/nalyk/azoth/pull/19) | Merged 2026-04-22 after 10 review rounds; Phase-5 reconcile pass + `parser_key: Result` self-healing on data-invariant violations; `Language::all_extractor_wired()` is the SSOT for PRs B/C/D to widen |
| B | Python tree-sitter | **merged 48b779c** | — | — | [#20](https://github.com/nalyk/azoth/pull/20) | Merged 2026-04-22 after 8 review rounds; tree-sitter-python 0.21; iterative `python::walk` (per-language; grammar-specific node kinds) sharing `common.rs` helpers (`short_digest`, `line_range`, `name_via_field`); `short_digest` hardened against inconsistent byte ranges; empty-wired SQL guard via `wired_placeholders()`; TreeCursor reuse across the walk |
| C | TypeScript tree-sitter | **merged 4a71ecf** | — | — | [#22](https://github.com/nalyk/azoth/pull/22) | Merged 2026-04-22 after 1 review round; tree-sitter-typescript 0.21 with `language_typescript()`/`language_tsx()` dual factories; iterative `typescript::walk` (per-language) reusing `common.rs` helpers + the TreeCursor-reuse pattern; `function_signature` + `method_signature` classifier arms unlock `.d.ts` / interface / `declare class` extraction; `parser_for` delegates to `parser_key` (single SSOT); fixture floors locked at 35/14/50/8/9/4. Pre-commit discipline hook architecturally fixed (JSON additionalContext) |
| D | Go tree-sitter | **merged fca7cd1** | — | — | [#23](https://github.com/nalyk/azoth/pull/23) | Merged 2026-04-22 after 1 review round; `tree-sitter-go = "0.21"`; iterative `go::walk` (per-language) reusing `common.rs` helpers + the TreeCursor-reuse pattern; `const_spec` comma-leak fix via `.is_named()` filter; rejected gemini's `package_clause.name` field claim after empirical verification (0.21.2 ships `fields={}`); codex clean pass round 0 |
| E | pytest TDAD | **merged 1fd43d4** | — | — | [#24](https://github.com/nalyk/azoth/pull/24) | Merged 2026-04-22 after 12 review rounds; R10 reversed Arc<str>/doc drift; R11 flipped argv so internal `-v --no-header --tb=short --color=no` win over user `extra_args` (pytest last-flag-wins); R12 used `TestId::as_str()` in cargo.rs sibling over `.0` field access for encapsulation. Codex approvals R3/R8/R9/R10/R11 (boilerplate / Bravo / Breezy / Keep it up / :rocket:). `PytestImpact` selector + `PytestRunner` live runner + shared `TestRunner` trait with `Arc<str>` detail + per-test `-v` parser. Key patterns saved to memory: `feedback_parser_rewrite_count_is_a_signal.md` (use `--json` from day one, 6 parser rewrites across R3-R8 is the signal) + `feedback_pragmatic_label_is_underdelivery_tell.md` + `pattern_graphql_reviewthreads_is_authoritative.md` (DUAL GraphQL+REST query pattern) |
| F | jest TDAD | **merged 489db53** | — | — | [#25](https://github.com/nalyk/azoth/pull/25) | Merged 2026-04-23 after 7 review rounds; `jest --json` structured output from day one (avoids PR-E text-parser trap); monorepo (workspaces/projects) → `JestError::UnsupportedConfig`; argv order user extra_args → internal flags (`--json --colors=false --silent`) → `--` → test paths; `word_boundary_contains` helper extracted at 3 sibling sites (jest/pytest/cargo) with 17 unit tests; null-safety ladder (R5) for nested JSON access; R6 per-test detail moved from global stderr pollution |
| G | go test TDAD | **merged f678066** | — | — | [#26](https://github.com/nalyk/azoth/pull/26) | Merged 2026-04-23 after 7 review rounds; ~25 findings closed + 5 v2.2 defers (parallel per-package exec, go.mod module-prefix parsing, go.work multi-module, -bench-aware runner, partial-failure resilience across pkgs). Production-quality Go TDAD: `go test -json` NDJSON from day one, parent-dir-path matching (not file-stem — Go's unit is the package), `word_boundary_contains` reused, `go.work` rejected, build-tags `extra_args` plumbed, Unicode test names (`TestÉclair`), benchmarks excluded (use `-bench`), cap keeps TAIL not head, go.mod/go.sum trigger select-all, plan.tests order preserved across packages, sorted subtest rollup for byte-stable forensic output. 756/756 workspace single-threaded; 4/4 live-tools with real go 1.25.6 |
| H | sandbox default flip (Off → TierA) | **merged 7add2c0** | — | — | [#27](https://github.com/nalyk/azoth/pull/27) | Merged 2026-04-23 after 4 review rounds; H1/H2 split not needed (only 1 implicit legacy-Off consumer in the audit); R0 initial flip caught gemini HIGH + codex P1 (unsafe `fork()` from Tokio multi-thread runtime); R1 added `OnceLock<bool>` probe cache + `warm_userns_cache()` from `fn main()` + swept sibling `bash.rs:642` to cached form + `OnceLock<()>` on the degradation warn (codex P1 progression on library-embedder gap); R2 added `RuntimeFlavor::MultiThread` guard in cached probe (codex P1 progression on non-tokio multithread gap); R3 generalized to **thread-identity** via `OnceLock<ThreadId>` — runtime-agnostic, catches `std::thread`, Rayon, any async runtime. New `pattern_safety_docstring_in_adversarial_review.md` saved: re-read SAFETY docstring for every function you add a NEW call site for. Also saved `feedback_user_controls_sprint_gate_handoff.md` — stop at sprint-gate boundary; user instructs next PR. |
| I | red-team corpus +20 | **merged 25c8f1d** | — | — | [#21](https://github.com/nalyk/azoth/pull/21) | Merged 2026-04-22 after 3 review rounds; 20 new cases in `src/red_team.rs` across 5 categories; exhaustive-origin check converted to `match` (compile-time enforcement); cfg-gated Unix-only symlink test |
| J | dogfood + 50-task eval seed | **merged e7817e5** | — | — | [#28](https://github.com/nalyk/azoth/pull/28) | Merged 2026-04-24 after 6 review rounds; gemini posted R0 findings across **5 waves** over 40 min (22:32/22:37/22:44/22:59/23:09) — R1-R5 closed waves progressively; R5 introduced `count_pattern` closure which drew fresh R5-era findings closed in R6 (UTF-8 boundary → `strip_prefix`, POSIX trailing newline). Codex clean-pass R0-R6 (7 consecutive boilerplate comments). All 10 gemini inline threads addressed in code. Shipment: 50-task hand-labelled seed (20 v2 + 10 Py + 10 TS + 10 Go; seed-mode localization@5 = 0.4783), 3 headless dogfood writeups (psf/requests / vscode-eslint / urfave/cli with 870/406/1232 Py/TS/Go symbols), `tests/eval_v2_1_seed.rs` gate test (50-task count, per-language partition via `count_pattern`, uniqueness via HashSet). **New discipline memory saved:** `feedback_dual_query_immediately_before_every_push.md` (DUAL-query GraphQL+REST within seconds of push; filter `isResolved == false` not push-time). |
| K | v2.1.0 version bump + tag | **merged a183791** | — | — | [#29](https://github.com/nalyk/azoth/pull/29) | Merged 2026-04-24 after 4 review rounds; workspace.version 2.0.2 → 2.1.0; `CHANGELOG.md` created with `[2.1.0]` section; README refreshed. R0 walker-location claim wrong (tracker had "common::walk_preorder" aspirational wording that never shipped — per-language `fn walk`, common.rs holds short_digest/line_range/name_via_field); R1 swept all 4 sibling sites + dated inline correction on historical session-log; R2 closed SemVer form `[1.5]`→`[1.5.0]`, Go language capitalization, silent Java removal (correct direction for phantom forward claim); R3 caught Case B failure from R2 — gemini's late wave 9:18 past trigger flagged that v2.0.2's README line 201 had publicly promised Java for v2.1, so silent removal was dishonest. R3 added explicit slip acknowledgement in CHANGELOG Non-scope. Codex clean-pass R0-R3 (*"Another round soon, please!"* → *"Chef's kiss"* → *"You're on a roll"* → *"Can't wait for the next one!"*). Gemini R3 top-level: *"I have no feedback to provide as there are no review comments."* New pattern memos saved: `pattern_grep_verify_release_notes_against_code.md` (code-shaped claims vs source tree) + `feedback_no_phantom_roadmap_claims.md` (scheduling claims two-rule framework: forward-phantom vs prior-promise-slip). Annotated tag `v2.1.0` pushed; release.yml workflow 24879051027 triggered. |

## Legend

- `pending` — not started.
- `open` — branch exists, PR opened, in review.
- `review` — bot findings posted; next push addresses them.
- `merged <sha>` — landed on `main`.

## Dependency graph (from plan)

```
  A ──┬─> B ──> E ──┐
      ├─> C ──> F ──┤
      └─> D ──> G ──┤
                    ├─> J ──> K
  H ───────────────>┤
  I ───────────────>┘
```

## Resumption preamble (paste after `/new`)

> Resuming v2.1.0 execution. Plan at
> `docs/superpowers/plans/2026-04-21-v2_1-implementation.md`;
> tracker at `docs/superpowers/plans/2026-04-21-v2_1-tracker.md`.
> Check `gh pr view <N>` for any in-flight PRs in the tracker.
> Continue from the first `pending` row whose dependencies are
> satisfied. Respect sprint-gate discipline
> (`project_v2_sprint_gate_discipline.md` in memory): don't open
> PR N+1 until N merges to `main`.

## Session log (append-only)

- **2026-04-24** — **PR-K (#29) MERGED to main as `a183791` after 4
  review rounds.** Annotated tag `v2.1.0` pushed; release.yml
  workflow `24879051027` triggered on the tag at 08:03:27 UTC.
  **v2.1.0 shipped — sprint complete.**

  Scope: workspace.version `2.0.2` → `2.1.0` (single edit; all three
  crates inherit via `[workspace.package]`); new `CHANGELOG.md` with
  a `[2.1.0]` section enumerating every public-surface change
  (additive `SymbolKind` variants, language dispatcher, tree-sitter
  Python/TypeScript/Go, pytest/jest/go-test TDAD, +20 red-team
  cases, 50-task eval seed, dogfood writeups, `AZOTH_SANDBOX`
  default flip); `README.md` refreshed (header version string,
  "honest status" bullets flipped from "v2.1 scope" to shipped,
  version-history prepends v2.1.0).

  Review cycle: 4 rounds. Codex boilerplate clean-pass every round
  (*"Another round soon, please!"* R0 → *"Chef's kiss"* R1 →
  *"You're on a roll"* R2 → *"Can't wait for the next one!"* R3).
  Gemini R3 top-level: *"I have no feedback to provide as there are
  no review comments."* All 8 gemini inline findings across 3 waves
  closed in code.

  Key findings + the Java lesson. R0 walker-location (2 MEDs on
  CHANGELOG.md:25 + README.md:425): `common::walk_preorder` was
  aspirational wording that entered the 2026-04-22 session-log
  resumption note, propagated into tracker status rows B/C/D, then
  into CHANGELOG + README — never shipped as a function. `common.rs`
  holds `short_digest` / `line_range` / `name_via_field` only; the
  walker stays per-language (`rust::walk`, `python::walk`,
  `typescript::walk`, `go::walk`). R1 swept all 4 sibling sites +
  added a dated inline correction on the 2026-04-22 session-log
  entry (preserves forensic history; doesn't rewrite it). New memo
  `pattern_grep_verify_release_notes_against_code.md` saved.

  R2 closed 3 wave-2 findings (SemVer form `[1.5]` → `[1.5.0]`,
  Go language capitalization, silent Java removal). I originally
  saved a memo `feedback_no_phantom_roadmap_claims.md` arguing
  "drop phantom roadmap claims." R3's late gemini wave (9:18 past
  trigger — outside the typical 5-6 min cadence, inside my
  conservative 4-min additional wait) proved the memo incomplete:
  v2.0.2's README line 201 had publicly promised Java for v2.1
  scope. Silent removal hid the slip from users who read v2.0.2's
  docs. R3 added an explicit slip acknowledgement in CHANGELOG
  Non-scope. Memo rewritten with two rules: (A) grep plan docs —
  drop unbacked forward-scope claims; (B) `git show vPREV:README.md |
  grep X` — acknowledge prior-promise slips even without a forward
  plan. Both rules compose to the correct R3 outcome.

  Late-wave pattern validated: `pattern_graphql_reviewthreads_is_authoritative.md`
  and `feedback_wait_for_bot_processing_after_rereview.md` — gemini
  posted waves at 07:22:06 (R0), 07:27:53 + 07:32:31 (R1 waves 1+2),
  07:46:04 (R2 late wave at +9:18), and the empty R3 review at
  08:01:03. Conservative 4-9 min wait before declaring clean-pass
  caught the R2→R3 late wave that push-time-only filters would have
  missed.

  Sprint complete: A (#19 4d72379) + B (#20 48b779c) + C (#22
  4a71ecf) + D (#23 fca7cd1) + E (#24 1fd43d4) + F (#25 489db53) +
  G (#26 f678066) + H (#27 7add2c0) + I (#21 25c8f1d) + J (#28
  e7817e5) + K (#29 a183791). All 11 tracker rows merged. v2.1.0
  tag pushed + release.yml in progress.

- **2026-04-24** — PR-J (#28) MERGED to main as `e7817e5` after 6
  review rounds. Scope: `docs/eval/v2.1_seed_tasks.json` (50 hand-
  labelled tasks, +30 over v2: 10 Python targeting psf/requests@93bf533,
  10 TypeScript targeting microsoft/vscode-eslint@93b96ab, 10 Go
  targeting urfave/cli@b79d768), 3 headless dogfood writeups under
  `docs/dogfood/v2.1/` driving `azoth eval run --live-retrieval`
  against each cloned repo (870/406/1232 Py/TS/Go symbols;
  5/5 + 4/5 + 5/5 short-identifier probe recall), and
  `crates/azoth-core/tests/eval_v2_1_seed.rs` gate test (50 tasks,
  per-language partition via `count_pattern` + HashSet uniqueness,
  seed-mode `localization@5 ≥ 0.45`, shipped at 0.4783).

  Review cycle: gemini R0 review ran as a **5-wave 40-minute stream**
  (22:32/22:37/22:44/22:59/23:09 UTC 2026-04-23). I triaged at 22:38
  seeing only waves 1+2, then pushed R1 without re-querying; each
  corrective round had to sweep new ground. Tally of findings
  addressed across R1-R6: 10 inline gemini threads, every one resolved
  in code (GitHub UI keeps them "unresolved" because gemini doesn't
  auto-resolve on re-review). Codex clean-pass on every round (R0
  "Another round soon, please!" → R1 "More of your lovely PRs please."
  → R2 "Keep it up!" → R3 "Bravo." → R4 ":rocket:" → R5 "Breezy!" →
  R6 "Delightful!"). Zero codex inline findings across 7 rounds.

  Key findings closed: dir-paths in 6 loc tasks' `relevant_files`
  (R1 + R2 sibling sweep on `predicted_files`); expect-message style
  ×3 (R1 + R3 + R4 with failure-mode wording); path-construction
  brittleness (R1 applied gemini's cheaper-alternative, R3
  rejected-with-docs against extraction refactor — no workspace
  helper in azoth-core, n=2 sites below n≥3 extraction threshold);
  task-ID uniqueness (R4 HashSet against eval_runs composite-PK
  collision); loose `starts_with` per-language count (R5
  `count_pattern` closure, R6 swapped to `strip_prefix` for UTF-8
  boundary safety); seed JSON missing POSIX trailing newline (R6).

  **New discipline memory saved** — `feedback_dual_query_immediately_before_every_push.md`:
  waiting longer for bot processing is necessary but not sufficient;
  the actionable invariant is DUAL-query GraphQL reviewThreads + REST
  newest *within seconds of push*, filter `isResolved == false` (not
  push-time timestamp), because a wave posted between triages gets
  missed by push-time filters but caught by unresolved filters.

  Sprint-gate leaves only **PR-K** (v2.1.0 tag + CHANGELOG + README
  version bump) pending. All prereqs for K are met; waiting on user
  kickoff per `feedback_user_controls_sprint_gate_handoff.md`.
- **2026-04-23** — PR-H (#27) MERGED to main as `7add2c0` after 4
  review rounds. Closing bot signals: gemini R3 verbatim *"I have
  no feedback to provide"*; codex R3 verbatim *"Codex Review:
  Didn't find any major issues. Bravo."* Round summary (R0→R3):
  R0 initial flip (gemini HIGH + codex P1: unsafe fork from tokio
  multi-thread runtime); R1 OnceLock<bool> probe cache + warm from
  main() + bash.rs:642 sibling + OnceLock<()> log-flood fix (codex
  P1 progression: library-embedder gap); R2 RuntimeFlavor guard
  in cached probe (codex P1 progression: non-tokio multithread gap);
  R3 thread-identity guard via OnceLock<ThreadId> — runtime-agnostic.
  Saved `pattern_safety_docstring_in_adversarial_review.md`
  (re-read SAFETY docstrings for every new call site) and
  `feedback_user_controls_sprint_gate_handoff.md` (stop at
  sprint-gate boundary, user manually kicks off next PR).
  Sprint-gate: remaining pending rows J (dogfood + 50-task eval
  seed; all prereqs B/C/D/E/F/G merged) and K (v2.1.0 tag; needs
  A-J shipped). Awaiting manual instruction to start PR-J.
- **2026-04-24** — PR-H R3 pushed. Both bots clean-pass at R2
  top-level (gemini "I have no feedback to provide"; codex
  boilerplate), but codex posted ONE inline P1 bot-progression:
  R2's tokio-only `RuntimeFlavor::MultiThread` check misses
  library embedders using `std::thread`, Rayon, or another async
  runtime — cold cache + those contexts still fork-from-
  multithreaded-process. Accepted the progression; generalized
  the guard to thread-identity so it catches ANY multi-threaded
  caller independent of runtime. Fix: `warm_userns_cache()` now
  records the calling thread as `WARM_THREAD` via `OnceLock<ThreadId>`;
  `probe_unprivileged_userns_cached()` on cold cache requires
  the current thread to match `WARM_THREAD` or fails closed
  with a one-shot `tracing::error`. Swapped R2's `tokio::runtime::Handle`-
  flavor check for the thread-id gate. Consequence: every test
  that transitively hits the cached probe needs to warm first
  — updated `sandbox_skip()` in bash.rs (sibling sweep for all
  tier-A/B tests), `from_env_defaults_to_tier_a_when_userns_available`
  in policy.rs, and all 3 tests in `sandbox_default_tier_a.rs`.
  Probe unit tests refreshed too (dropped 3 tokio-flavor tests,
  added 2 thread-identity tests: cold-call-from-warming-thread +
  cross-thread-determinism). 763/763 workspace green single-
  threaded. Clippy + fmt clean (fixed one `redundant_closure`
  in the cross-thread test). Adversarial self-review per
  `pattern_safety_docstring_in_adversarial_review.md` grepped
  the probe's SAFETY docstring — new design satisfies it by
  enforcing single-threaded-context dynamically via thread-id
  rather than trusting embedders to follow a written rule.
- **2026-04-23** — PR-H R2 pushed. Codex clean-pass on R0; gemini
  clean-pass on R1 ("I have no feedback to provide"). Codex R1
  posted **one P1 bot-progression**: the R1 cache pre-warm only
  covers the `azoth` binary — library embedders who use `azoth-core`
  directly (SDK, daemon, hypothetical future harness) can still
  trigger the first probe from inside a multi-thread tokio runtime
  with a cold cache, violating the safety invariant R1 was meant
  to close. Accepted the progression — this is sibling-scope, not
  a duplicate. R2 fix: `probe_unprivileged_userns_cached()` now
  refuses to fork when called with a cold cache from inside a
  **multi-thread** tokio runtime (via `Handle::try_current()` +
  `RuntimeFlavor::MultiThread` check). Cold-call-in-multi-thread
  returns `false` with a one-shot `tracing::error` naming
  `warm_userns_cache()`. Cold-call-outside-tokio and cold-call-
  under-current-thread-tokio both continue to fork safely — the
  existing `#[tokio::test]` suite in bash.rs uses the default
  `current_thread` flavor, so those tests keep working. Added 3
  new unit tests (outside-tokio / current-thread / multi-thread)
  that empirically pin the three branches. 764/764 workspace
  green single-threaded (+3 from R1's 761). fmt + clippy clean.
- **2026-04-23** — PR-H R1 pushed as `cd8e0b3`. Both bots
  independently flagged the same CRITICAL: my R0 routed
  `probe_unprivileged_userns()` through `SandboxPolicy::from_env()`,
  which gets called from `BashTool::execute` under the Tokio
  multi-threaded runtime. That violates the probe's SAFETY
  precondition (`fork()` from a multi-threaded process is
  POSIX-restricted and glibc-locks can deadlock the child).
  Gemini HIGH and codex P1 named the same fix: cache the probe
  once. I missed this in adversarial self-review even though I had
  read the SAFETY docstring — the kind of failure
  `feedback_adversarial_self_review_before_push.md` exists to
  catch. Fix: new `probe_unprivileged_userns_cached()` backed by a
  `OnceLock<bool>`, `warm_userns_cache()` helper called from
  `fn main()` before Tokio starts, and BOTH probe call sites
  (policy.rs new + pre-existing bash.rs:642) routed through the
  cached form — sibling-site sweep per
  `pattern_extract_helper_over_inline_reviewer_patch.md`. Gemini
  MED on log flooding: also addressed — wrapping the
  user-ns-unavailable `tracing::warn` in a `OnceLock<()>` so the
  warning fires at most once per process. 761/761 workspace green
  single-threaded (+2 new probe tests: cache consistency +
  warm idempotency). fmt + clippy clean.
- **2026-04-23** — PR-H opened on `feat/v2_1-H` from `main @ b8e35b7`.
  Scope: flip `SandboxPolicy::from_env()` default Off → TierA (with
  graceful degradation to Off via `probe_unprivileged_userns()` when
  the host can't `unshare(CLONE_NEWUSER)`). H1-audit found exactly
  ONE test relying on the legacy-Off default implicitly
  (`bash_default_policy_still_writes_to_repo_root_regression_guard`
  at `tools/bash.rs:891`). Well below the 10-test threshold, so no
  H1/H2 split — shipped as a unified PR. That test pre-sets
  `AZOTH_SANDBOX=off` and was renamed to
  `bash_explicit_off_still_writes_to_repo_root_regression_guard`
  to reflect its new role (pins Off semantics, not default
  semantics). Policy module header, the `from_env()` doc, and the
  3 unit tests updated in the same commit. New integration test
  `crates/azoth-core/tests/sandbox_default_tier_a.rs` with 3 cases
  (unset → TierA-when-userns-else-Off probe-conditional; empty env
  matches unset; explicit off → Off). README updated: new "v2.1
  default change" section above the tier table, the intro
  "sandbox is on" paragraph flipped from "when on" to "by default",
  the "honest status" line flipped from "opt-in" to "on by default
  / opt out", and the "defaults to off" caveat reversed. 759/759
  workspace green single-threaded (+3 from 756 on main). fmt +
  clippy clean. Bot review pending.
- **2026-04-23** — PR-G (#26) MERGED to main as `f678066` after 7
  review rounds. R6 closed 4 more (gemini HIGH cap keep-tail, codex
  P1 go.mod/go.sum select-all, codex P2 plan.tests order
  preservation, gemini MED deterministic subtest rollup sort) + 1
  v2.2 defer (partial-failure resilience with PR-J threshold
  trigger). Final tally: ~25 findings closed + 5 explicit v2.2
  defers across the full PR. Sprint-gate now unblocks PR-H (sandbox
  default flip, independent) and PR-J (dogfood + 50-task eval seed,
  now that all of B/C/D/E/F/G are merged). PR-K (v2.1.0 tag) still
  last, needs A-J. Remaining work: H, J, K.
- **2026-04-23** — PR-G (#26) progressed through R0 → R5 this session.
  R0 shipped ba2a3d6 (first drop) + 759f683 (tracker sync); R1 ad71c32
  closed 5 findings + 1 defer (parallel exec → v2.2); R2 ef7d600 closed
  3 findings (full-path HIGH, helper deletion, ancestor walk); R3
  bd8d926 closed 3 findings (two-pass agg, Discovery cap sibling-swept
  across 2 sites via shared cap_to_max_bytes, root-level .go
  fallback); R4 4bb2f86 closed 4 findings (build-tags extra_args HIGH
  with live-tools regression test, Unicode-aware name matcher P2
  matching Go's `!unicode.IsLower(first_rune)` semantic, clone→move on
  ev.test, slot-cap between-passes via extracted cap_in_place
  primitive now reused at 3 sites); R5 tightened live-tools runner
  assertion to pin TestGamma specifically. Total ~16 closed + 1
  explicitly deferred with scope. Codex reviewed at R0 + R3 only
  (silent R1/R2/R4/R5 — queues selectively). Gemini reviewed every
  round and found real improvements each time. Running v2.2 revisit
  backlog: parallel per-package exec, go.mod module-prefix parsing,
  go.work multi-module, -bench-aware runner. 750/750 workspace
  single-threaded; 3/3 live-tools tests pass with go 1.25.6. Pattern
  memo `pattern_go_test_json_schema_and_count_cache.md` enriched with
  5 gotchas (exit-1 ambiguity, subtest keying, -run vs -bench,
  Unicode test names, -count=1 cache). Next: push R5, wait for
  R6 bots, sync tracker as LAST commit before merge.
- **2026-04-23** — PR-F (#25) MERGED to main as `489db53` after 7 review
  rounds. Tracker row synced (was stale at `open`/`pending push`).
  Sprint-gate unblocks PR-G (go test TDAD, D merged), PR-H (sandbox flip,
  independent). Per tracker ordering, starting PR-G. Branch
  `feat/v2_1-G` cut from `main @ 489db53`. PR-F process wins applied to
  PR-G upfront: (a) structured output from day one — `go test -json`
  (not the plan's text-block parsing of `-list=.*` + `ok PKG` state
  machine, per `feedback_parser_rewrite_count_is_a_signal.md`); (b)
  reuse `word_boundary_contains` helper from PR-F instead of re-introducing
  substring-contains false positives (per
  `pattern_extract_helper_over_inline_reviewer_patch.md`); (c) argv order
  user extra_args → internal flags → `--` → test paths (PR-E R11 pattern).
- **2026-04-22** — PR-E (#24) MERGED to main as `1fd43d4` after 12
  review rounds. R10 (`029f10a`) fixed doc/code drift on Arc<str>
  (R9 gemini MED on the tracker itself — the log still claimed
  Arc<str> was deferred after R4 shipped it). R11 (`326a7e9`)
  flipped argv order so internal parser flags (`-v --no-header
  --tb=short --color=no`) win over user `extra_args`, defending
  against a user who supplies `-q` or `--color=yes` silently
  breaking our output parser (gemini HIGH on R10). R12 (`c4b89a2`)
  swapped `TestId.0` field access for `TestId::as_str()` in the
  `cargo.rs` sibling — encapsulation parity with the pytest path.
  Codex round signals across the PR: R3 boilerplate-only, R8
  *"Didn't find any major issues. Bravo."*, R9 *"Breezy!"*, R10
  *"Keep it up."*, R11 *"🚀"*. 12 rounds is a lot; root cause is
  text-parsing `pytest -v` stdout — saved `feedback_parser_rewrite_count_is_a_signal.md`
  to memory: if round N ≥ 3 and each round fixes a different edge
  case in the same parser, stop and look for `--junitxml` / `--json`
  / structured output from day one. PR-F will use `jest --json`
  from the first commit. Sprint-gate now unblocks PR-F (jest, C ✅),
  PR-G (go test, D ✅), PR-H (sandbox flip, independent). Starting
  PR-F per tracker ordering. Branch `feat/v2_1-F` cut from `main`.
- **2026-04-22** — PR-E rounds R4-R9 pushed across this session
  segment. R4 (`757570f`) addressed codex P1 parametrize parser
  bug + gemini MED ×2 Arc<str> escalation (R3 defer reversed).
  R5 (`81e093d`) `.trim()` test-id padding + stderr-only missing-
  pytest signal + `--color=no` on both pytest invocations sibling.
  R6 (`651afc9`) rightmost-word-boundary parser + `ERROR
  collecting` anchor on dep helper + `MAX_DETAIL_BYTES` const.
  R7 (`0ff1638`) strip trailing `(reason)` block + exit-code
  dispatch (only 0/1 proceed to outcome parsing). R8 (`33a5de7`)
  balanced-paren scan for nested reasons (R7 `rfind('(')` only
  stripped innermost). R9 (`f10fa88`) POSIX `--` separator before
  test IDs to defend against parametrize values producing leading-
  `-` node ids. **9 review rounds total**; pytest parser rewritten
  6 times. Process insight saved as
  `feedback_parser_rewrite_count_is_a_signal.md`: text parsing of
  human-readable subprocess output is wrong by round 3; switch to
  structured output (`pytest --junitxml`) from day one. Codex
  approval signals seen: boilerplate-only review (R3), *"Didn't
  find any major issues. Bravo."* (R8), *"Didn't find any major
  issues. Breezy!"* (R9). Gemini's GraphQL `reviewThreads`
  returned EMPTY 6 consecutive verifications (R4-R9); REST sort-
  by-created-desc was authoritative each time. Saved as
  `pattern_graphql_reviewthreads_is_authoritative.md` with DUAL-
  query pattern. Workspace 648/648 green; clippy + fmt clean.
- **2026-04-22** — PR-E R3 pushed as `6797a33` at 12:13 UTC.
  **Codex P2** on R2 (`2b5e2f2`): my dependency-error helper scanned
  for bare `ImportError` / `ModuleNotFoundError` substrings, which
  would misfire on test bodies with `pytest.raises(ImportError)`
  that didn't raise. Narrowed to pytest's literal collection
  reporter wrapper `"ImportError while importing"` (does not occur
  in user test output) plus interpreter-bootstrap
  `"No module named 'pytest'"`. 2 false-positive guard tests lock
  the narrowing. **Gemini HIGH** on R2: escalated MED→HIGH the
  "one failure sinks all" mapping that my R2 commit body had
  labelled "pragmatic v2.1 shape". Reversed in 30 lines: switched
  `pytest -q` → `pytest -v`, added `parse_pytest_verbose_outcomes`
  tokeniser with per-status mapping (PASSED|XFAIL → Pass; FAILED|
  ERROR|XPASS → Fail; SKIPPED → Skip; missing test ids → absent →
  caller uses `TestOutcome::Unknown`). Also reused `merge_pipes`
  for runner detail construction (gemini R2 top-level summary).
  Initially deferred `Arc<str>` for `TestRunResult.detail` to
  v2.1.1 with a "blast radius widens across PR-F/G" rationale.
  **REVERSED in R4** (`757570f`) after gemini escalated the
  finding from summary-only advisory to inline MED ×2: the blast
  radius is the SAME whether shipped now or later (PR-F/G inherit
  the trait shape either way), so deferring just adds churn. R4
  changed `TestRunResult.detail: Option<String>` →
  `Option<Arc<str>>`. R9 gemini MED on this tracker line caught
  the doc-vs-code drift that this paragraph created when R4
  landed without amending the R3 log entry.
  638/638 workspace green single-threaded (+6 from R2: 2 false-
  positive guards, 4 parser tests, minus 2 broad-helper tests
  replaced). Codex R3 reviewed `6797a33` at 12:17 with no
  suggestions (clean pass). Gemini R3 at 12:14: verbatim *"this
  looks ready for merge. Great work on this PR."*
  
  **Process correction saved to memory** (`feedback_wait_for_bot_processing_after_rereview.md`):
  I declared R2 done at 11:45 (1 min after /gemini review trigger),
  but bots posted at 11:47 (codex) and 11:49 (gemini) with 2 NEW
  findings I missed. User re-asked "unaddressed issues on PR #24"
  → failure signal. R3 enforced via `ScheduleWakeup(delaySeconds=360)`
  to wait 6 min before verifying bot responses. Also saved
  `feedback_pragmatic_label_is_underdelivery_tell.md` — sibling to
  `restraint-is-earned`: "pragmatic" in a commit body is a smell
  that I might be dressing up a usability gap as a virtue.
- **2026-04-22** — PR-E R1 pushed as `4963743`, then R2 as
  `2b5e2f2`. R1 addressed 5 gemini findings (HIGH char-boundary
  truncate + 2× redundant `exists()` + `HashSet<TestId>` + consequent)
  and self-caught identical `HashSet<String>` pattern in
  `cargo.rs:204` via sibling audit (fixed atomically). R2
  addressed codex P1/P2 (pytest collection errors emit on stdout
  not stderr — `ImportError while importing` wrapper) + gemini
  MED re-raise (`HashSet<TestId>` → `HashSet<&str>` strictly
  zero-alloc; applied to both pytest + cargo siblings) + gemini
  MED ARG_MAX deferred with inline docs (v2.2 batching + revisit
  trigger). 4 new unit tests on R2 helper; 632/632 green.
- **2026-04-22** — PR #23 merged to `main` as `fca7cd1`; local
  `feat/v2_1-D` was already cleaned up during prior session.
  Sprint-gate unblocks PR-E (pytest), PR-F (jest), PR-G (go test),
  and PR-H (sandbox flip) simultaneously — picking PR-E per the
  memory-preferred default ("PR-E next"). Branch `feat/v2_1-E`
  cut from main.
- **2026-04-22** — PR-E opened as [#24](https://github.com/nalyk/azoth/pull/24)
  on `feat/v2_1-E` @ `8ea8950`. Scope: `PytestImpact` selector +
  `PytestRunner` live runner + shared `TestRunner` trait. Three
  plan-doc API drifts caught up front per
  `pattern_verify_plan_doc_apis_against_code.md`: (1) `TestUniverse`
  already lives in `cargo.rs` → reused, not shadowed; (2) plan's
  `pub use super::pytest::PytestError;` self-reference at the end
  of `pytest.rs` (which the plan doc itself flags as a bug) elided;
  (3) plan's manual `impl Default for PytestRunner` collapsed to
  `#[derive(Default)]` per `clippy::derivable_impls`; (4)
  `TestRunner::run` takes `&Path` instead of the plan's `&PathBuf`
  for idiomatic Rust + clippy pedantic. New `live-tools` Cargo
  feature gates the pytest-agreement test so CI stays green on
  hosts without pytest on PATH. Workspace 627/627 green
  single-threaded (+12 from 615 on main). Clippy + fmt clean. Live
  pytest-agreement test NOT run on this host (pytest absent) —
  flagged in PR body. Bot re-review pending; don't open PR-F
  (jest, blocked on C — already merged) or any other pending row
  until PR-E merges to respect sprint-gate.
- **2026-04-22** — PR-D R1 pushed as `d75460f`. Two findings on `398abd6`
  (round 0): **Codex** clean pass ("No major issues. Chef's kiss.").
  **Gemini** MED 3123418508 on `package_clause` — claimed 0.21 exposed
  a `name` field, suggested `name_via_field(&node, "name", bytes)`.
  Verified empirically (memory:
  `feedback_verify_review_claims_empirically.md`) by applying the patch
  and running `go_extraction::package_emits_symbol` — failed with
  "package symbol missing: []". On 0.21.2 (Cargo.lock) `fields={}` on
  `package_clause`; gemini's review body referenced 0.21.0 which MAY
  have had the field but we're pinned forward. Rejected with 15-line
  inline doc per `feedback_reject_with_documentation_when_arch_forbids.md`.
  **Sideband win**: running the verification spilled the full
  extractor output into view and caught a bug I shipped in the
  original `cb94b4b` — `const One, Two, Three = 1, 2, 3` was emitting
  5 Const symbols because `children_by_field_name("name")` on
  `const_spec` returns `,` separators alongside identifiers (grammar
  types `[identifier(named), ","(named:false)]` with `multiple:true`).
  Fixed via `.is_named()` filter + regression test asserting no
  punctuation-only symbols leak. Sibling audit: no other
  `children_by_field_name` sites in the workspace. Bots re-triggered.
- **2026-04-22** — PR-D opened as [#23](https://github.com/nalyk/azoth/pull/23)
  on `feat/v2_1-D` @ `cb94b4b`. `tree-sitter-go = "0.21"` pinned;
  `code_graph/go.rs` lands with the iterative walker +
  `common.rs` helpers reuse; dispatcher arms wired; path-aware
  `parser_key(Language::Go, ..)` returns `Ok(ParserKey::Go)`;
  `all_extractor_wired()` widened to the 4-grammar v2.1 set.
  **Node-types enumeration done before implementation** (per memory
  `pattern_tree_sitter_classifier_enumerate_node_types.md`), caught
  two plan-draft misses: `type_alias` (Go 1.9+ `type X = Y` is a
  distinct grammar node) and `method_elem` (interface members do
  NOT reuse `method_declaration`). Both covered by new tests that
  lock the fixes + parent linkage. `const_spec` uses
  `children_by_field_name("name")` because the grammar marks it
  `multiple: true` for the `const A, B = 1, 2` parallel form.
  Sentinel-test cleanup: deleted 5 rotated sentinels (plan counted
  3; found 2 more in `indexer.rs`). Each deletion has an in-place
  comment naming why-empty-now + when-to-reintroduce so a future
  `Language`-variant addition before extractor wiring knows the
  sentinel contract. Replaced with `extract_for_go_dispatches_to_go_extractor`
  + `parser_for_go_returns_real_parser`, mirroring PR-B's Python
  coverage. 615/615 workspace green single-threaded (up from 603
  on main). Bot reviews pending; don't open PR-E (pytest TDAD,
  blocked on B) or begin any other pending row until PR-D merges.
- **2026-04-22** — PR #22 merged to main as `4a71ecf`; local
  `feat/v2_1-C` deleted; remote `origin/feat/v2_1-C` pruned; 603/603
  workspace tests green on main. Sprint-gate unblocks PR-D (Go) as
  the next pending row.
- **2026-04-22** — PR-C review round 1 pushed as `231ce19`. Addressed
  2 codex P1s (`function_signature` + `method_signature` missing from
  classifier — silently dropped `.d.ts` / interface / `declare class`
  members) and 1 gemini MED (`parser_for` duplicated
  `parser_key`'s `.ts`/`.tsx` discrimination; now delegates via
  `parser_key(lang, path)?`, single SSOT). Failing-first TDD on all
  4 new tests (declare function, overload signatures, interface
  method sigs, declare class method sigs). Fixture count floors
  bumped from 20/10/20/5/5/3 to 35/14/50/8/9/4 to lock the
  extraction surface against future classifier-narrowing
  regressions. 603/603 workspace green (+4 from 599). Pre-commit
  discipline hook now fires correctly after architectural fix
  (exit-0-stderr → JSON additionalContext surface); walked all 7
  checkpoints before pushing. Bot re-review requested via single
  top-level comment + inline replies on each of 3 threads.
- **2026-04-22** — PR-C opened on `feat/v2_1-C`. Scope landed cleanly —
  tree-sitter-typescript 0.21 dep pin (API is
  `language_typescript()`/`language_tsx()` functions, NOT the plan's
  `LANGUAGE_TYPESCRIPT`/`LANGUAGE_TSX` consts which are 0.22+-only);
  `code_graph::typescript` module with iterative walker reusing
  `common.rs` helpers (short_digest, line_range, name_via_field);
  dispatcher arms split for extract_for + path-aware parser_for;
  `all_extractor_wired()` widened to include TypeScript; 3 PR-A
  sentinel tests rotated TypeScript→Go (pending until PR-D).
  Iterative walker's class-nesting depth test replaced with
  nested-function-declarations (TS grammar doesn't admit nested class
  declarations inside class bodies, unlike Python). 599/599 workspace
  green single-threaded (+54 from PR-B). Bot reviews pending; don't
  open PR-D until PR-C merges.
- **2026-04-22** — Resumption. PR-B (#20) merged as `48b779c` (8 review
  rounds) and PR-I (#21) merged as `25c8f1d` (3 review rounds). Main @
  `25c8f1d`. Sprint-gate unblocks PR-C (TypeScript); branch
  `feat/v2_1-C` cut from main. Plan reconciliation note: PR-B landed
  with shape that deviates from the plan — iterative walker in
  `code_graph/common.rs` with `walk_preorder(&mut cursor, visit)`,
  HashMap parser cache in indexer, `Language::all_extractor_wired()`
  SSOT, `wired_placeholders()` empty-set guard, hardened
  `short_digest` against inconsistent byte ranges. PR-C must reuse
  these (not duplicate), and the plan's recursive-walker stub in
  `typescript.rs` must be rewritten to the shared iterative shape.
  *[2026-04-24 correction (PR-K R1): `common::walk_preorder` was never
  extracted — each language ships its own `fn walk` (tree-sitter node
  kinds are grammar-specific). What `common.rs` actually holds is
  `short_digest`, `line_range`, `name_via_field`. The TreeCursor-reuse
  pattern is the shared convention, not a shared function. Gemini
  caught this on PR #29 R0.]*
- **2026-04-22** — PR-I opened on `feat/v2_1-I` @ `45027f5` (parallel
  to PR-B review cycle per "independent" status). Scope: 20 red-team
  cases across 5 categories (path-traversal, unicode, FTS prompt-
  escape, symbol shell-meta, origin-spoofing) routed through the
  `pub(crate)`-correct home `src/red_team.rs`. Plan doc's
  `Tainted::new_for_test` API didn't exist; pattern captured in
  `pattern_verify_plan_doc_apis_against_code.md`. 550/550 workspace
  green single-threaded.
- **2026-04-21** — Plan committed (`6ef75c0`). PR-A opened as #19
  (`9b38e93`). Session checkpoint: v35 memo
  (`project_azoth_status_apr21_v35_*`). Tier B parallel-flake pattern
  captured. Clean `/new` point taken here.
- **2026-04-21** — PR-A fixup `830eaa5` addresses gemini MED on
  `Language::from_wire` (non-idiomatic `Some(match { _ => return None })`
  flipped to per-arm `Some(Variant)` / `None`). Both bot reviews
  re-requested. 213/213 azoth-core + 63/63 azoth-repo lib green.
- **2026-04-22** — PR-B opened as [#20](https://github.com/nalyk/azoth/pull/20)
  at head `b6d90b8`. Scope landed cleanly — tree-sitter-python 0.21
  dep pin, `code_graph::python` module, dispatcher arms split, backfill
  loop re-bound to `all_extractor_wired()`. 3 PR-A pending-language
  regression tests rotated Python→TypeScript sentinel. 545/545 workspace
  green single-threaded (+22 from PR-A). `malformed_input` test honest
  about tree-sitter-python INDENT/DEDENT recovery limits — stricter
  variant (`malformed_trailing_garbage_does_not_panic`) asserts only
  no-panic + ≥1 symbol. Bot reviews pending. Next: watch for gemini +
  codex feedback; don't open PR-C until #20 merges.
- **2026-04-22** — PR-A merged as `4d72379`. 10 review rounds;
  round-9 parser_key flipped from `unreachable!()` to
  `Result<ParserKey, ExtractError>` after gemini's 4th raise (discipline
  failure memoed in `feedback_reject_with_documentation_when_arch_forbids.md`).
  Round-10 Phase-5 reconcile closed the mtime-unchanged drift gap.
  Starting PR-B (Python tree-sitter) on branch `feat/v2_1-B`. Scope is
  tighter than plan anticipated: indexer already dispatcher-driven, so
  PR-B is (1) add `tree-sitter-python` dep, (2) create
  `code_graph/python.rs`, (3) swap Python arm of `extract_for`/`parser_for`,
  (4) append Python to `all_extractor_wired()`, (5) widen backfill
  predicate to use the wired slice as SSOT.
- **2026-04-21** — PR-A round 2 `8e67e9b` addresses 3 gemini MEDs on
  the `830eaa5` review: (1) added
  `ExtractError::UnsupportedLanguage(Language)` variant so dispatcher
  arms stop overloading the ABI-failure `ExtractError::Language`
  docstring; (2) symmetric taxonomy locked by new
  `parser_for_pending_languages_errors` test; (3) indexer main loop
  now routes through `Language::from_wire` +
  `code_graph::parser_for` + `code_graph::extract_for` (parser cache
  widened from `Option<Parser>` to `HashMap<Language, Parser>`, so
  PRs 2.1-B/C/D only need to touch the dispatcher arm in
  `code_graph::mod.rs`). New integration test
  `pending_grammar_file_indexed_without_symbols` empirically proves
  zero behaviour regression. Plan doc synced (5 pending-sense refs
  flipped; 4 ABI-sense refs kept). 523/523 workspace green.
